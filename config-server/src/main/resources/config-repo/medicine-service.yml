server:
  port: 8082

spring:
  datasource:
    url: jdbc:h2:mem:medicinedb
    driverClassName: org.h2.Driver
    username: root
    password: huy0965507655
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka/
